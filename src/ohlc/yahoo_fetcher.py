# yahoo_fetcher.py
class YahooFetcher:
    """Fetches OHLC data on-demand (never stored in DB)."""

    def fetch(self, ticker, start, end):
        # TODO: Real Yahoo fetch
        return {"ohlc": []}
